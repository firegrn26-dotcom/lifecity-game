LifeCity 2.5D Stage 5 — проверка сборки и исправления

Проверено:
- JS-синтаксис всех клиентских модулей.
- JS-синтаксис server.js и local-server.js.
- Порядок подключения модулей в public/index.html.
- Загрузка клиентских скриптов в браузерном окружении со стабами canvas/socket.
- Первый вызов draw() без ReferenceError.
- Дубли зданий в карте: не обнаружены.
- Завод recyclingFactory: присутствует один раз.
- Лишние вложенные папки/старые копии проекта в архиве: не обнаружены.

Исправлено:
- npm run check теперь проверяет все новые модули public/js/*.js, а не только старый public/game.js.
- Базы data/*.json очищены до стартового состояния, чтобы тестовые аккаунты, чат и старый мусор не попадали в чистую сборку.

Важно:
- Серверная логика не менялась.
- Если нужно сохранить старых игроков, перед заменой проекта скопируй свои реальные файлы из папки data.
