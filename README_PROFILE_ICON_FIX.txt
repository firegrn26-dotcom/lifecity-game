LifeCity — Profile Icon Fix

Исправлено:
- значок/аватар игрока в окне профиля больше не смещается и не "убегает";
- аватар теперь закреплен в экранных координатах карточки профиля;
- добавлена маска/область под аватар, чтобы он не вылезал за блок;
- убрана зависимость от положения камеры при отрисовке аватара в профиле.

Проверка:
- public/game.js прошел node --check.

Запуск:
1. npm install
2. npm start
