LifeCity Auth Button Fix

Исправлено:
- кнопки «Вход» и «Регистрация» теперь продублированы настоящими DOM-кнопками поверх canvas;
- погодные эффекты, canvas и музыкальный autoplay-патч больше не могут перехватывать клики авторизации;
- логика осталась прежней: неактивная кнопка переключает режим, активная выполняет вход/регистрацию.

Запуск:
1) npm install
2) npm start
3) открыть http://localhost:3000
